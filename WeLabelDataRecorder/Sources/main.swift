import AppKit

// Simple application entry point to get started
print("WeLabelData Recorder - Starting up...")

let app = NSApplication.shared
let delegate = AppDelegate()
app.delegate = delegate

app.run() 